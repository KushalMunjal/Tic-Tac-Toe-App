//now let's add the color that we will need to project
import 'package:flutter/material.dart';

class MainColor {
  static Color primaryColor = Color(0xFF00061a);
  static Color secondaryColor = Color.fromARGB(255, 2, 18, 72);
  static Color accentColor = Color.fromARGB(255, 12, 17, 34);
}